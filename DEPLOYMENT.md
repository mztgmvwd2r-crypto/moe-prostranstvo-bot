# Развёртывание на Web Service

Руководство по развёртыванию бота "Моё пространство" на различных облачных платформах.

---

## Выбор платформы

### Рекомендуемые платформы

| Платформа | Бесплатный план | Сложность | Рекомендация |
|-----------|----------------|-----------|--------------|
| **Railway** | $5 кредит | ⭐ Легко | ✅ Лучший выбор |
| **Render** | 750 часов/мес | ⭐⭐ Средне | ✅ Хорошо |
| **Fly.io** | 3 VM бесплатно | ⭐⭐ Средне | ✅ Хорошо |
| **Heroku** | Платно | ⭐ Легко | ⚠️ Нет бесплатного плана |
| **DigitalOcean** | От $4/мес | ⭐⭐⭐ Сложно | Для опытных |

---

## 🚂 Railway (Рекомендуется)

Railway — самый простой способ развернуть бота с минимальными настройками.

### Шаг 1: Подготовка

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Подключите GitHub аккаунт

### Шаг 2: Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `moe-prostranstvo-bot`
4. Railway автоматически определит Python проект

### Шаг 3: Настройка переменных окружения

В разделе **Variables** добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=ваш_openai_ключ
WEBHOOK_URL=https://ваш-проект.up.railway.app
```

**Важно:** `WEBHOOK_URL` будет доступен после первого деплоя. Сначала деплойте без него, потом добавьте и передеплойте.

### Шаг 4: Деплой

1. Railway автоматически начнёт деплой
2. Дождитесь завершения (2-3 минуты)
3. Скопируйте URL вашего сервиса
4. Добавьте `WEBHOOK_URL` с этим URL
5. Передеплойте (нажмите "Redeploy")

### Шаг 5: Проверка

1. Откройте `https://ваш-проект.up.railway.app/health`
2. Должно вернуться: `{"status": "healthy", "bot": "moe_prostranstvo"}`
3. Протестируйте бота в Telegram

---

## 🎨 Render.com

### Шаг 1: Подготовка

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Подключите GitHub

### Шаг 2: Создание Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий `moe-prostranstvo-bot`
3. Настройки:
   - **Name**: `moe-prostranstvo-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn bot_webhook:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120`

### Шаг 3: Переменные окружения

В разделе **Environment** добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен
OPENAI_API_KEY=ваш_ключ
WEBHOOK_URL=https://ваш-сервис.onrender.com
```

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь деплоя (5-7 минут)
3. Проверьте статус на `/health`

**Примечание:** Бесплатный план Render засыпает после 15 минут неактивности. Первый запрос может занять 30-60 секунд.

---

## ✈️ Fly.io

### Шаг 1: Установка CLI

```bash
curl -L https://fly.io/install.sh | sh
```

### Шаг 2: Авторизация

```bash
fly auth login
```

### Шаг 3: Создание приложения

```bash
cd moe-prostranstvo-bot
fly launch --no-deploy
```

Ответьте на вопросы:
- App name: `moe-prostranstvo-bot` (или ваше имя)
- Region: выберите ближайший
- Database: No
- Deploy: No

### Шаг 4: Настройка переменных

```bash
fly secrets set TELEGRAM_BOT_TOKEN="ваш_токен"
fly secrets set OPENAI_API_KEY="ваш_ключ"
fly secrets set WEBHOOK_URL="https://ваше-приложение.fly.dev"
```

### Шаг 5: Деплой

```bash
fly deploy
```

### Шаг 6: Проверка

```bash
fly open /health
```

---

## 🐳 Docker (Универсальный способ)

Подходит для любой платформы, поддерживающей Docker.

### Локальное тестирование

```bash
# Создайте .env файл
cat > .env << EOF
TELEGRAM_BOT_TOKEN=ваш_токен
OPENAI_API_KEY=ваш_ключ
WEBHOOK_URL=https://ваш-домен.com
EOF

# Запустите с Docker Compose
docker-compose up -d

# Проверьте логи
docker-compose logs -f

# Проверьте здоровье
curl http://localhost:8080/health
```

### Деплой на любую платформу

```bash
# Соберите образ
docker build -t moe-prostranstvo-bot .

# Запустите контейнер
docker run -d \
  -p 8080:8080 \
  -e TELEGRAM_BOT_TOKEN="ваш_токен" \
  -e OPENAI_API_KEY="ваш_ключ" \
  -e WEBHOOK_URL="https://ваш-домен.com" \
  -v $(pwd)/data:/app/data \
  --name moe-bot \
  moe-prostranstvo-bot
```

---

## 🔧 Настройка Webhook

После деплоя нужно настроить webhook Telegram.

### Автоматическая настройка

Бот автоматически настраивает webhook при запуске, если установлена переменная `WEBHOOK_URL`.

### Ручная настройка (если нужно)

```bash
curl -X POST "https://api.telegram.org/bot<ваш_токен>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ваш-домен.com/<ваш_токен>"}'
```

### Проверка webhook

```bash
curl "https://api.telegram.org/bot<ваш_токен>/getWebhookInfo"
```

Должно вернуться:
```json
{
  "ok": true,
  "result": {
    "url": "https://ваш-домен.com/...",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

---

## 📊 Мониторинг

### Health Check

Все платформы поддерживают health check на эндпоинте:

```
GET /health
```

Ответ:
```json
{
  "status": "healthy",
  "bot": "moe_prostranstvo"
}
```

### Логи

**Railway:**
```bash
railway logs
```

**Render:**
Смотрите в веб-интерфейсе: Logs → Live Logs

**Fly.io:**
```bash
fly logs
```

**Docker:**
```bash
docker logs -f moe-bot
```

---

## 🔒 Безопасность

### Переменные окружения

**Никогда не коммитьте:**
- `TELEGRAM_BOT_TOKEN`
- `OPENAI_API_KEY`

Используйте переменные окружения платформы.

### Webhook URL

Держите токен в секрете. Webhook URL содержит токен:
```
https://ваш-домен.com/<TELEGRAM_BOT_TOKEN>
```

Только Telegram должен знать этот URL.

---

## 💾 База данных

### Текущее решение (JSON)

Бот использует JSON файлы в папке `data/`:
- `users.json`
- `diary.json`
- `daily_energy.json`

**Ограничения:**
- Данные могут потеряться при рестарте (на некоторых платформах)
- Не подходит для большого количества пользователей

### Рекомендации для продакшена

Для постоянного хранения данных используйте:

**1. Railway PostgreSQL**
```bash
railway add postgresql
```

**2. Render PostgreSQL**
Добавьте PostgreSQL в веб-интерфейсе

**3. Supabase (бесплатно)**
- Зарегистрируйтесь на [supabase.com](https://supabase.com)
- Создайте проект
- Используйте PostgreSQL URL

**Миграция на PostgreSQL** требует изменения кода в `utils/database.py`.

---

## 🚀 Быстрый старт (Railway)

Самый быстрый способ развернуть бота:

1. **Fork репозиторий** на GitHub
2. Откройте [Railway.app](https://railway.app)
3. **New Project** → **Deploy from GitHub**
4. Выберите форкнутый репозиторий
5. Добавьте переменные:
   ```
   TELEGRAM_BOT_TOKEN=...
   OPENAI_API_KEY=...
   ```
6. Дождитесь деплоя
7. Скопируйте URL сервиса
8. Добавьте переменную:
   ```
   WEBHOOK_URL=https://ваш-проект.up.railway.app
   ```
9. **Redeploy**
10. Готово! 🎉

---

## ❓ Проблемы и решения

### Бот не отвечает

**Проверьте:**
1. Webhook настроен: `curl https://api.telegram.org/bot<токен>/getWebhookInfo`
2. Сервис запущен: `curl https://ваш-домен.com/health`
3. Логи на наличие ошибок

### Ошибка 502 Bad Gateway

**Причина:** Сервис не запустился или упал

**Решение:**
1. Проверьте логи
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что порт правильный (используйте `$PORT`)

### Webhook не устанавливается

**Причина:** URL должен быть HTTPS

**Решение:**
- Все облачные платформы предоставляют HTTPS автоматически
- Для локального тестирования используйте ngrok

### Данные теряются при рестарте

**Причина:** Эфемерная файловая система

**Решение:**
- Используйте volume/persistent storage
- Или мигрируйте на PostgreSQL

---

## 📝 Сравнение режимов

### Polling (bot.py)

**Плюсы:**
- Простая настройка
- Работает локально
- Не нужен публичный URL

**Минусы:**
- Постоянное подключение к Telegram
- Не подходит для serverless
- Больше нагрузка на API

### Webhook (bot_webhook.py)

**Плюсы:**
- Эффективнее
- Подходит для облака
- Меньше запросов к API

**Минусы:**
- Нужен HTTPS URL
- Сложнее настройка
- Не работает локально (без ngrok)

---

## 🎯 Рекомендации

**Для начала:** Railway (самый простой)

**Для продакшена:**
- Railway + PostgreSQL
- Render + PostgreSQL
- DigitalOcean + Docker + PostgreSQL

**Для тестирования:** Локально с `bot.py` (polling режим)

---

**Удачного деплоя! 🌿**
