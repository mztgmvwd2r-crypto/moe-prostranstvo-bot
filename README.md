# Моё пространство — Telegram Bot

**Тихое пространство поддержки и подсказок**

Telegram-бот для женщин с функциями Таро, энергии дня и личного дневника. Создан с мягким, поддерживающим подходом без предсказаний судьбы.

## Особенности

### 🃏 Таро
- **Режим бота**: бот сам вытягивает карты (1 или 3 карты)
- **Своя колода** (Premium): интерпретация карт из физической колоды пользователя
- Мягкие, поддерживающие интерпретации
- Вопросы для дневника после каждого расклада

### ⭐ Энергия дня
- Короткий астро-фон
- Карта дня с интерпретацией
- Мягкий совет
- Вопрос для размышления

### 📝 Дневник
- Личные записи мыслей и ощущений
- Сохранение раскладов Таро и энергии дня
- Архив записей (полный доступ для платных подписчиков)
- Темы и паттерны (Premium)

### 🔔 Уведомления
- Ежедневная энергия дня
- Напоминания о записях в дневник

### ✨ Подписка
- **FREE**: базовые функции с лимитами
- **BASE** (₽299/мес): больше раскладов, архив, углублённые интерпретации
- **PREMIUM** (₽599/мес): всё из Base + своя колода + темы и паттерны

## Технологии

- **Python 3.11+**
- **python-telegram-bot** — для работы с Telegram Bot API
- **OpenAI API** — для генерации интерпретаций Таро и энергии дня
- **JSON** — для хранения данных пользователей

## Структура проекта

```
moe_prostranstvo/
├── bot.py                  # Основной файл бота
├── requirements.txt        # Зависимости Python
├── .env.example           # Пример файла с переменными окружения
├── README.md              # Документация
├── data/
│   ├── tarot_deck.py      # Колода Таро (78 карт)
│   ├── users.json         # База данных пользователей
│   ├── diary.json         # База данных дневников
│   └── daily_energy.json  # Кэш энергии дня
└── utils/
    ├── database.py        # Утилиты для работы с данными
    └── ai_generator.py    # Генерация контента через AI
```

## Установка и настройка

### 1. Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота: `Моё пространство`
4. Введите username бота (должен заканчиваться на `bot`): например, `moe_prostranstvo_bot`
5. Сохраните полученный **токен** — он понадобится для запуска

### 2. Установка зависимостей

```bash
cd moe_prostranstvo
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` или установите переменную окружения:

```bash
export TELEGRAM_BOT_TOKEN="your_bot_token_from_botfather"
```

OpenAI API ключ уже настроен в окружении (переменная `OPENAI_API_KEY`).

### 4. Запуск бота

```bash
python3 bot.py
```

Бот запустится и будет ожидать сообщений. Вы увидите сообщение:
```
Bot started! Press Ctrl+C to stop.
```

## Использование

### Команды

- `/start` — начать работу с ботом, показать приветствие

### Главное меню

После `/start` появится главное меню с кнопками:
- **⭐ Энергия дня** — получить энергию текущего дня
- **🃏 Таро** — задать вопрос и получить расклад
- **📝 Дневник** — создать запись или просмотреть записи
- **🔔 Уведомления** — настроить уведомления
- **✨ Подписка** — посмотреть планы подписки

## Лимиты для бесплатных пользователей

- **Энергия дня**: 1 раз в день
- **Таро**: 1 расклад в день
- **Дневник**: последние 5 записей

## Развёртывание на сервере

### Вариант 1: Запуск через systemd (Linux)

Создайте файл `/etc/systemd/system/moe-prostranstvo.service`:

```ini
[Unit]
Description=Moe Prostranstvo Telegram Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/moe_prostranstvo
Environment="TELEGRAM_BOT_TOKEN=your_token_here"
Environment="OPENAI_API_KEY=your_openai_key_here"
ExecStart=/usr/bin/python3 /home/ubuntu/moe_prostranstvo/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable moe-prostranstvo
sudo systemctl start moe-prostranstvo
sudo systemctl status moe-prostranstvo
```

### Вариант 2: Запуск через Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

Создайте `docker-compose.yml`:

```yaml
version: '3.8'

services:
  bot:
    build: .
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```

Запустите:

```bash
docker-compose up -d
```

### Вариант 3: Запуск через screen (простой способ)

```bash
screen -S moe_prostranstvo
cd /home/ubuntu/moe_prostranstvo
export TELEGRAM_BOT_TOKEN="your_token"
python3 bot.py
# Нажмите Ctrl+A, затем D для отключения от screen
```

Вернуться к боту:
```bash
screen -r moe_prostranstvo
```

## База данных

Бот использует JSON-файлы для хранения данных:

- **users.json** — информация о пользователях, подписках, лимитах
- **diary.json** — записи дневников
- **daily_energy.json** — кэш энергии дня

Файлы создаются автоматически при первом запуске.

## Интеграция платежей

В текущей версии платёжная система не интегрирована. Для добавления платежей:

1. **Telegram Payments API** — встроенная система платежей Telegram
2. **ЮKassa** — российская платёжная система
3. **Stripe** — международная платёжная система

Пример интеграции с Telegram Payments:

```python
from telegram import LabeledPrice

async def send_invoice(update, context):
    await context.bot.send_invoice(
        chat_id=update.effective_chat.id,
        title="Подписка BASE",
        description="Больше раскладов, архив, углублённые интерпретации",
        payload="base_subscription",
        provider_token="YOUR_PAYMENT_PROVIDER_TOKEN",
        currency="RUB",
        prices=[LabeledPrice("Подписка BASE", 29900)]  # в копейках
    )
```

## Мониторинг и логи

Логи сохраняются в консоль. Для сохранения в файл:

```bash
python3 bot.py >> bot.log 2>&1
```

Или используйте systemd для автоматического логирования:

```bash
sudo journalctl -u moe-prostranstvo -f
```

## Безопасность

1. **Никогда не коммитьте токены** в Git
2. Используйте `.env` файлы или переменные окружения
3. Ограничьте доступ к файлам данных:
   ```bash
   chmod 600 data/*.json
   ```
4. Регулярно делайте резервные копии данных

## Резервное копирование

Создайте скрипт для резервного копирования:

```bash
#!/bin/bash
tar -czf backup_$(date +%Y%m%d).tar.gz data/
```

Настройте cron для автоматического бэкапа:

```bash
0 3 * * * /path/to/backup_script.sh
```

## Обновление бота

1. Остановите бота
2. Сделайте резервную копию данных
3. Обновите код
4. Установите новые зависимости: `pip install -r requirements.txt`
5. Запустите бота снова

## Поддержка и контакты

Для вопросов и предложений свяжитесь с разработчиком.

## Лицензия

Этот проект создан для личного использования.

---

**Помни: бот помогает услышать себя, а не предсказывает судьбу 🤍**
